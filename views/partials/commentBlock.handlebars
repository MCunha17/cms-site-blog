{{#each comments as |comment|}}
<div class="comment-block">
    <h5>{{comment.username}}</h5>
    <p>{{comment.comment_text}}</p>
    <small>Posted on: {{format_date comment.date_created}}</small>
</div>
{{/each}}